name: OSW / Home / Publish

on:
  repository_dispatch:
    types: [publish-osw-home]
jobs:
  publish:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      deployments: write

    env:
      TAG_NAME: ${{ github.event.client_payload.tag }}

    steps:
      - name: Set version env variable
        run: echo "VERSION=${TAG_NAME#osw-home-v}" >> $GITHUB_ENV

      - name: Checkout
        uses: actions/checkout@v4

      - name: Install pnpm
        uses: pnpm/action-setup@v4
        with:
          version: latest
          run_install: false

      - name: Install Node.js
        uses: actions/setup-node@v4
        with:
          cache: 'pnpm'

      - name: Install dependencies
        run: pnpm install

      - name: Build
        run: pnpm nx run osw/home:build:production
        env:
          OSW_HOME_URL: https://home.open-source.thalesrc.com

      - name: Create firebase.json
        run: |
          cat > firebase.json << 'EOF'
          {
            "hosting": {
              "site": "osw-home",
              "public": "dist/osw/home",
              "ignore": ["firebase.json", "**/.*", "**/node_modules/**"],
              "rewrites": [
                {
                  "source": "**",
                  "destination": "/index.html"
                }
              ],
              "headers": [
                {
                  "source": "**",
                  "headers": [
                    {
                      "key": "Access-Control-Allow-Origin",
                      "value": "https://open-source.thalesrc.com"
                    },
                    {
                      "key": "Access-Control-Allow-Methods",
                      "value": "GET, HEAD, OPTIONS"
                    },
                    {
                      "key": "Access-Control-Allow-Headers",
                      "value": "Content-Type"
                    }
                  ]
                },
                {
                  "source": "**/*.@(js|css|woff2|woff|ttf)",
                  "headers": [
                    {
                      "key": "Cache-Control",
                      "value": "public, max-age=31536000, immutable"
                    }
                  ]
                }
              ]
            }
          }
          EOF

      - name: Deploy to Firebase Hosting
        uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT }}'
          projectId: ${{ secrets.FIREBASE_PROJECT_ID }}
          channelId: live
          target: osw-home
