{
  "name": "@thalesrc/auto-proxy",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "library",
  "tags": ["auto-proxy", "docker", "reverse-proxy", "nginx", "ssl"],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "docker build -t thalesrc/auto-proxy:latest .",
        "cwd": "libs/auto-proxy"
      }
    }
  }
}
