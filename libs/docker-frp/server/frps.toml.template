# FRP Server Configuration Template
# This configuration will be processed by the entrypoint script

# Basic server configuration
bindPort = ${BIND_PORT}

# Virtual host ports for HTTP/HTTPS proxies
vhostHTTPPort = ${VHOST_HTTP_PORT}
vhostHTTPSPort = ${VHOST_HTTPS_PORT}

# Dashboard configuration
[webServer]
addr = "0.0.0.0"
port = ${DASHBOARD_PORT}
user = "${DASHBOARD_USER}"
password = "${DASHBOARD_PASSWORD}"

${AUTH_SECTION}
