# @telperion/ng-ui/form/field

`FieldComponent` (selector `tui-field`) and `FieldModule`.

```ts
import { FieldModule } from '@telperion/ng-ui/form/field';
```

`FieldComponent` is **not** standalone — import `FieldModule` to declare it.
