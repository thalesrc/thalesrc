# @telperion/ng-ui/form

Form-level building blocks for `@telperion/ng-ui`.

This entry point currently exposes a placeholder; concrete form-level APIs will land here as the library grows. The nested `field` submodule is published separately at [`@telperion/ng-ui/form/field`](./field/README.md).
